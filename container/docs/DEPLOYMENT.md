# Deployment Guide

The Podman deployment guide lives at [../DEPLOYMENT.md](../DEPLOYMENT.md).

Keep a single copy to avoid drift: update `container/DEPLOYMENT.md`, and this
file continues to point at it.
