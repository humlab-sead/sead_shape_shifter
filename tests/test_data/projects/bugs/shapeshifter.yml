metadata:
  name: bugs
  type: shapeshifter-project
  description: Configuration for debug tests of shapeshifter.
  version: 1.0.0
  default_entity: survey
entities:
  a:
    type: fixed
    public_id: a_id
    keys: []
    columns: [system_id, a_type, a_id, a_value]
    values:
    - [1, 'A', 1, 'a']
  b:
    type: fixed
    public_id: b_id
    keys: []
    columns: [system_id, b_type, b_id, b_value]
    values:
    - [1, 'B', 1, 'b']
  ab:
    type: fixed
    public_id: ab_id
    keys: []
    columns: [system_id, ab_type, ab_id, ab_value]
    values: []
    append:
    - source: a
      columns: [system_id, a_type, a_id, a_value]
    - source: b
      columns: [system_id, b_type, b_id, b_value]
options:
