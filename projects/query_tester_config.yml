# Test Configuration for Query Tester
# This configuration is minimal and only includes data sources for testing

options:
  data_sources:
    # SQLite test database with sample data
    test_sqlite:
      driver: sqlite
      database: "projects/test_query_tester.db"
    
    # PostgreSQL example (requires actual database)
    # sead_production:
    #   driver: postgresql
    #   host: localhost
    #   port: 5432
    #   database: sead_production
    #   username: postgres
    #   password: password

# Minimal entities section (required by ConfigStore)
entities: {}
