# What's New in v1.21.0

Date: 2026-02-21

## Highlights

- Add column suggestions to Keys and Columns fields.
- Add copy to clipboard functionality for validation results.
- Clarify auto-detect-columns feature logic in SQL entity type.
- Clarify comment on layout response model filtering.
- Add type assertion for position style in GraphNodeContextMenu.


## New Features

- Add column suggestions to Keys and Columns fields.
- Add copy to clipboard functionality for validation results.
- Add data validation mode option for sample and complete validation ([#226](https://github.com/humlab-sead/sead_shape_shifter/issues/226)).
- Add dual-source file listing with location field.
- Add error handling and notifications for Excel metadata and columns fetching ([#228](https://github.com/humlab-sead/sead_shape_shifter/issues/228)).
- Add Excel sheet nodes to dependency graph and refactor file extractors.
- Add extra columns management in ForeignKeyEditor component.
- Add feature to edit value replacements in Entity Editor.
- Add file path resolution with location field support.
- Add form data change watcher to trigger validation in EntityFormDialog.
- Add item-value binding to column items in ForeignKeyEditor.
- Add loading overlay to EntityFormDialog while fetching entity data.
- Add methods to enable and disable foreign key suggestions in settings.
- Add PreviewError component and integrate error handling in EntityDataPreview.
- Add project refresh feature to reload from disk ([#229](https://github.com/humlab-sead/sead_shape_shifter/issues/229)).
- Add QueryFilter class for filtering DataFrames using Pandas query method.
- Add refresh functionality to reload project from disk ([#229](https://github.com/humlab-sead/sead_shape_shifter/issues/229)).
- Add regex_sub support to Replacements editor.
- Add Replacements tab and editor to Entity Editor with YAML integration.
- Add static validation for unnest and foreign key configurations.
- Add static validation specifications for runtime error prevention.
- Add support for materialized entities in dependency graph and update styles.
- Add support for previewing unsaved entity configurations in preview_entity ([#214](https://github.com/humlab-sead/sead_shape_shifter/issues/214)).
- Add system_id auto-fix strategies.
- Add transform rule for applying normalize/coerce to all values.
- Add transform rule support to Replace tab.
- Add validation for dependent entities in DependsOnResolvedSpecification.
- Auto-load schema and data preview on table selection.
- Enhance data source API to support inline configurations and string references ([#219](https://github.com/humlab-sead/sead_shape_shifter/issues/219)).
- Enhance foreign key editing experience with column introspection and directive validation ([#202](https://github.com/humlab-sead/sead_shape_shifter/issues/202)).
- Enhance project loading with ApplicationState cache support.
- Enhance sanitize_column_name to handle Excel formulas and enforce max length.
- Enhance schema handling and improve SQL query generation in entity generator service.
- Enhance schema introspection API with new endpoints for table listing and schema retrieval using data source config.
- Fix for using extra_columns as local_keys in chained foreign keys.
- Hide file-based data sources from Data Source Type dropdown.
- Implement entity generation from database tables and add corresponding UI components ([#217](https://github.com/humlab-sead/sead_shape_shifter/issues/217)).
- Implement filter API endpoints and dynamic form generation.
- Implement foreign key merge setup to avoid duplicate columns.
- Implement get_entity_columns method for improved column retrieval in specifications.
- Implement global configuration check for YAML intelligence in Monaco editor.
- Implement project copy functionality with UI dialog.
- Implement retryWithBackoff function for handling transient failures.
- Improve SQL query generation in SqlLoader by refining limit and offset handling.
- Move get_excel_metadata and _parse_cell_range_max_column functions for Excel file processing.
- Optimize preview generation by implementing caching mechanism in task status.
- Prevent system_id and public_id in columns field for fixed entities.
- Refactor and update user guide.
- Remove validation button from project detail view. It's the same check as "STRUCTURAL" in "VALIDATE" tab.
- Replace _resolve_data_source_config with load_data_source for improved data source handling.
- Replace search field and table list with combobox in schema explorer.
- Replace textarea with Monaco editor for SQL query input and enhance editor options ([#215](https://github.com/humlab-sead/sead_shape_shifter/issues/215)).
- Support nested project paths (e.g., 'category/project-name').
- Update button labels for clarity and add options for data validation.
- Update DataValidationService to validate resolved project entities ([#223](https://github.com/humlab-sead/sead_shape_shifter/issues/223)).


## Improvements

- Clarify auto-detect-columns feature logic in SQL entity type.
- Clarify comment on layout response model filtering.
- Clarify FK validation issue regarding columns added by prior FKs.
- Clarify single worker requirement to prevent data loss in ApplicationState.
- Clarify source column label in ExtraColumnsEditor.
- Enable Save button when editing YAML in entity editor.
- Enhance dependency graph refresh logic after project update.
- Enhance entity loading logic in EntityFormDialog to handle changes in entity name.
- Enhance project validation to handle missing include files gracefully.
- Enhance watch logic to reset form on mode change.
- Improve exception handling in API endpoints and directive validation.
- Improve fixed entity values validation for backward compatibility.
- Improve FK validation ([#212](https://github.com/humlab-sead/sead_shape_shifter/issues/212)).
- Is dirty not saved for nested editors (save not enabled).
- Update docstring for ForeignKeyColumnsSpecification to clarify FK linking context.


## Fixes

- Add type assertion for position style in GraphNodeContextMenu.
- Add type ignore for data_validator_factory in ValidationService instantiation.
- Auto-add system_id and public_id to fixed entity columns.
- Auto-reload YAML editor when project changes externally.
- Bundle Monaco locally to resolve worker and tracking warnings.
- Cast column_types to Any for type safety in sead_column_dtypes.
- Correct data source service initialization to use GLOBAL_DATA_SOURCE_DIR.
- Correct directory paths for global data source discovery.
- Correct string formatting in error logging for entity count mismatch.
- Correct system_id and public_id offset in fixed entity editor.
- Correct TypeScript types in EntityFormDialog.
- Defensive fix for foreign key editing.
- Duplicate column for materialized entities.
- Eliminate redundant entity fetch causing race condition.
- Ensure cell edits are committed immediately in FixedValuesGrid.
- Ensure column names are strings to avoid NaN/float issues in DataFrames.
- Ensure safe extraction of filename from [@include](https://github.com/include) directive in data source config.
- Ensure string directives in columns and keys are not split into characters.
- Ensure table selection event fires correctly with nextTick.
- Extra columns doesn't open ([#209](https://github.com/humlab-sead/sead_shape_shifter/issues/209)).
- Fallback to SELECT * if no columns detected in SQL query generation.
- Fix Monaco worker creation errors.
- Fixed paging and vertical layout of preview table.
- Force reload project state after raw YAML update to prevent stale data.
- Handle both old and new fixed entity value formats.
- Handle column ranges in read-only mode for openpyxl.
- Handle errors during Monaco YAML configuration and disable hover/validation.
- Honor cell range when fetching metadata columns.
- Logic for  autodect columns is flawed ([#210](https://github.com/humlab-sead/sead_shape_shifter/issues/210)).
- Map FK merge keys through rename_map after column renaming.
- Move imports to module level to prevent UnboundLocalError.
- Normalize v-combobox keys to strings in ForeignKeyEditor.
- Preserve actual system_id values in FixedValuesGrid.
- Prevent double-submission of entity save.
- Prevent empty columns with values in fixed entities.
- Prevent entity dialog from closing on save.
- Prevent infinite loop freeze when selecting columns in Replace tab.
- Prevent infinite loop in Replace tab (complete fix).
- Prevent state updates after component unmounting.
- Refactor entity validation to use TableConfig for entity type retrieval.
- Refresh dependency graph after project refresh.
- Refresh entity list after project refresh.
- Remove unused 'select' from useProjects composable.
- Rename validation button to 'Check Project' for clarity.
- Resolve file paths for preview override config based on location field.
- Resolve file paths in execute workflow.
- Resolve portable relative paths from global data directory.
- Return portable relative paths for global data files.
- Sanitize column names when loading Excel sheets.
- Show detailed error messages on project deletion failures.
- Skip column validation for unnesting of SQL data in SqlLoader.
- Strip timezone info from datetime columns before Excel export.
- Sync project state after entity operations.
- Trim whitespace when converting column names to strings in sanitize_column_name.
- Truncate table descriptions to 49 characters in schema explorer.
- Unlock entity editor tabs immediately after creating entity.
- Update documentation links from UI_REQUIREMENTS.md to REQUIREMENTS.md.
- Update fetchProjectFiles to use correct query parameters for file extensions.
- Use GLOBAL_DATA_DIR for listing Excel files in entity creation.
- Use same buttons for create and edit modes ([#231](https://github.com/humlab-sead/sead_shape_shifter/issues/231)).


## Upgrade Notes

- Draft only: review whether this release changes recommended workflows, configuration, or upgrade steps.

## Learn More

- Technical changelog: CHANGELOG.md
- GitHub release: https://github.com/humlab-sead/sead_shape_shifter/releases/tag/v1.21.0
- Relevant docs: docs/USER_GUIDE.md
